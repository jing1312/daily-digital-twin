# 仓库统一用 LF 存储，检出时按下面的规则转换
* text=auto eol=lf

# Windows PowerShell 5.1 对编码很敏感：
#   - 没有 BOM 的 UTF-8 会按本地代码页解码，中文提示直接变乱码
#   - 混用 LF 会让 5.1 的部分解析路径出问题
# 所以 .ps1 一律检出为 CRLF；BOM 本身是文件内容，Git 会原样保留。
*.ps1 text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制资源不要做换行转换
*.png binary
*.jpg binary
*.ico binary
*.sqlite binary
